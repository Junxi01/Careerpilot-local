## Database

SQL schema and seed files live here.

- `schema.sql`: table definitions (initial baseline)
- `seed.sql`: optional local seed data

Migrations will be added later once the domain model stabilizes.
